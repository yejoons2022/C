#include <stdio.h>

main()
{
	int num, bel, nbel, type;
	int total = 1;
	int dire = 0;

	scanf("%d", &num);

	for(int i = 0; i < num; i++)
	{
		scanf("%d %d %d",&bel, &nbel, &type);
		
		if(type == 0)
		{
			printf ()
		}
	}
} 
